/**
 * Lightweight module declarations for `swiper` CSS imports so TypeScript
 * accepts importing those styles without additional type packages.
 *
 * Keep this file minimal — it only declares the CSS module names used in
 * the project.
 *
 * @module src/types/swiper
 */
declare module "swiper/css";
declare module "swiper/css/navigation";
declare module "swiper/css/pagination";
